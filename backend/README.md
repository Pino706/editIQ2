# Legacy Backend Snapshot

The active EditIQ API lives in `app/` and is mounted by `api/index.py` for Vercel.

This `backend/app/` tree is an older snapshot kept for reference only. Do not add new product work here unless you are deliberately migrating code out of it. New routes, models, database changes, and frontend integrations should target the root `app/` package.
